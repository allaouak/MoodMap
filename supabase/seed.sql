-- Seed de développement uniquement — NE PAS exécuter en production
-- Données de test pour un utilisateur fictif

-- Note : ce seed requiert un utilisateur déjà créé via auth.users
-- Utiliser l'interface Supabase pour créer le compte test, puis lancer ce seed

-- Les données sont insérées ici pour illustration uniquement
-- En production, les données sont créées via l'application

select 'Seed de dev — créer un compte via l''app puis insérer les entrées test manuellement' as message;
